{
  "name": "airtable",
  "version": "1.1.4",
  "description": "Airtable integration nodes for Cognigy.AI — read, insert, upsert, and schema inspection",
  "main": "build/module.js",
  "scripts": {
    "transpile": "tsc -p .",
    "lint": "tslint -c ../../tslint.json src/**/*.ts",
    "build": "npm run transpile && npm run lint && npm run zip",
    "zip": "tar cfz $npm_package_name-$npm_package_version.tar.gz build/* package.json package-lock.json README.md icon.png"
  },
  "keywords": ["Cognigy.AI", "extensions", "airtable", "database"],
  "author": "Ben Elliot <ben.elliot@nice.com>",
  "license": "ISC",
  "dependencies": {
    "@cognigy/extension-tools": "^0.16.1",
    "axios": "^1.7.9"
  },
  "devDependencies": {
    "@types/node": "^13.13.15",
    "tslint": "^6.1.3",
    "typescript": "^4.8.4"
  }
}
